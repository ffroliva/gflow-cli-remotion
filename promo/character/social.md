# `gflow character` — social copy (v0.12.0)

Post copy for the 4 × 9:16 promo variants. One block per hook angle so the copy
matches the rendered video (`CharacterPromo-<hook>` → MP4). Pair each block with
its matching MP4.

Links used throughout:

- Release: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
- Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md

---

## question — "What if your AI character looked the same in every shot?"

### X

> What if your AI character looked the same in every shot?
>
> `gflow character create` generates one reusable character — face reference,
> front/side/back body, voice, personality — from a single command. Reuse it
> across every generation. No drift.
>
> `pip install -U gflow-cli`
>
> Release: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
> Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md
>
> #AI #VideoGen #CLI #GoogleFlow

### LinkedIn

> What if your AI character looked the same in every shot?
>
> Consistency is the hard part of AI media. The new `gflow character` command in
> gflow-cli v0.12.0 creates a reusable character in one shot: a face reference,
> a front/side/back triptych body, a voice, and a personality — all bound to a
> Google Flow entity you can reuse across generations.
>
> One command. One consistent character. Straight from your terminal.
>
> Install / upgrade: `pip install -U gflow-cli`
> Release notes: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
> Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md
>
> #AI #VideoGen #CLI #GoogleFlow

---

## pain — "Your AI subject keeps changing face. Fix it in one command."

### X

> Your AI subject keeps changing face between shots. 😩
>
> Fix it in one command. `gflow character create` locks in a reusable character
> — face, body (front/side/back), voice — so it stays consistent everywhere.
>
> `pip install -U gflow-cli`
>
> Release: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
> Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md
>
> #AI #VideoGen #CLI #GoogleFlow

### LinkedIn

> Your AI subject keeps changing face between shots. Fix it in one command.
>
> Character drift is the #1 reason AI video looks "off." gflow-cli v0.12.0 adds
> `gflow character` — generate a reusable character once (face reference,
> front/side/back body, voice, personality) and reuse it across every
> generation. No re-prompting, no drift.
>
> `pip install -U gflow-cli`
> Release notes: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
> Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md
>
> #AI #VideoGen #CLI #GoogleFlow

---

## flex — "One command → a reusable character: face, body, voice."

### X

> One command → a reusable character: face, body, voice.
>
> `gflow character create --name Marina --face-prompt "…" --voice Kore
> --personality "…"` — and you have a consistent character bound to Google Flow,
> ready to reuse.
>
> `pip install -U gflow-cli`
>
> Release: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
> Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md
>
> #AI #VideoGen #CLI #GoogleFlow

### LinkedIn

> One command → a reusable character: face, body, voice.
>
> gflow-cli v0.12.0 ships `gflow character`. A single command generates a face
> reference, a front/side/back triptych body, and binds a voice + personality —
> all into a reusable Google Flow character entity. Define once, reuse
> everywhere, straight from your terminal.
>
> `pip install -U gflow-cli`
> Release notes: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
> Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md
>
> #AI #VideoGen #CLI #GoogleFlow

---

## dev — "Consistent characters, straight from your terminal."

### X

> Consistent characters, straight from your terminal.
>
> `gflow character create` reverse-engineers Google Flow's character entity:
> face + front/side/back body + voice + personality, reusable across
> generations. Scriptable, no clicking.
>
> `pip install -U gflow-cli`
>
> Release: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
> Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md
>
> #AI #VideoGen #CLI #GoogleFlow

### LinkedIn

> Consistent characters, straight from your terminal.
>
> For developers building AI media pipelines: gflow-cli v0.12.0 adds
> `gflow character` — create a reusable, consistent character (face reference,
> front/side/back body, voice, personality) with one command and reuse it across
> every Google Flow generation. Fully scriptable, no manual clicking.
>
> `pip install -U gflow-cli`
> Release notes: https://github.com/ffroliva/gflow-cli/releases/tag/v0.12.0
> Docs: https://github.com/ffroliva/gflow-cli/blob/main/docs/CHARACTER.md
>
> #AI #VideoGen #CLI #GoogleFlow
